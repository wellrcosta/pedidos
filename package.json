{
  "name": "pedidos",
  "version": "1.0.0",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --coverage --coverageReporters=json-summary"
  },
  "dependencies": {},
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
